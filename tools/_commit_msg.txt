feat: speak unique ability, unit, building, improvement in SelectCivilization (VP-SELECTCIV-RICHLABEL-1)

Each civ entry in the SelectCivilization popup now announces:
- Base title (leader, civ name, trait short name) — from VP's Title control
- Unique ability full description — from VP's BonusDescription control
- Unique unit (+ "replaces X" suffix) — lazy DB query, pcall-protected
- Unique building (+ "replaces X" suffix) — lazy DB query, pcall-protected
- Unique improvement — lazy DB query, pcall-protected

All DB queries are deferred to show-time (inside the monkeypatched
AddCivilizationEntry, called by VP's InitCivSelection via ShowHideHandler).
No DB access at include-time. Every query step is individually guarded
by pcall; failures are logged with Log.warn and the remaining parts
are still spoken. Random Civ is unchanged (no DB queries).

Label prefixes (Unique ability, Unique unit, ...) try CVA InGame
TXT_KEY_CIVVACCESS_UNIQUE_* keys first; fall back to hardcoded English
if not loaded in the FrontEnd context.

- update src/vp-compat/UI/FrontEnd/CivVAccess_VP_SelectCivilizationAccess.lua
  (new MD5 7E9523DF)
- update src/vp-compat/CivVAccess_VoxPopuli.modinfo (new MD5 for wrapper)
- update tools/validate-vp-compat.ps1: +8 AUTO checks for SelectCivilization
  and CivVAccess_VP_SelectCivilizationAccess (path, import, MD5, sentinel,
  lazy init, pcall, fallback string, bridge target) + 2 MANUAL items
- update CHANGELOG.md under [Unreleased]
- deploy to MODS/vp-compat verified (MD5 OK for all 4 changed files)

All 37 [AUTO] checks pass.
